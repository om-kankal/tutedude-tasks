The hamburger icon is placed inside a button.

The mobile menu is its immediate sibling.

This project uses only HTML and CSS.

The desktop navigation links are hidden on mobile and replaced with a hamburger icon.