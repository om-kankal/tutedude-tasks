Laundry Service Hero Section

HTML and CSS project

It was built using inline-block layout properties .

index.html: Contains the page layout, including the navigation bar and the hero section.

style.css: Contains CSS rules to style the layout using inline